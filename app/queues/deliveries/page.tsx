import QueuePlaceholder from "../QueuePlaceholder";

export default function DeliveriesQueuePage() {
  return (
    <QueuePlaceholder
      title="納品管理"
      description="発注済みで納品確認が残っている案件を表示する画面です"
    />
  );
}
